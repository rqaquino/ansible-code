name: ROLES DEPLOY OF NGINX
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  ansible-deploy:
    runs-on: self-hosted

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install Ansible Role
        run: |
          ansible-galaxy install -p /home/ronald/ansible/roles   geerlingguy.nginx
      
      - name: Run Ansible playbook
        env:
          ANSIBLE_HOST_KEY_CHECKING: False
        run: |
          pwd
          cp /home/ronald/ansible/inventory.yaml  /home/ronald/ansible/roles/inventory.yaml 
          cp /home/ronald/ansible/ansible.cfg  /home/ronald/ansible/roles/ansible.cfg 
          ansible-playbook  ./ansible/site.yaml -i /home/ronald/ansible/inventory.yaml  --become
