name: Run Ansible Playbook
on:
 push:
   branches: [main]
 pull_request:
   branches: [main]
 workflow_dispatch:
  

jobs:
  ansible-playbook:
    runs-on: [ ansible-master ] 

    steps:
      - uses: actions/checkout@v4

      - name: Execute Ansible Playbook
     
        run: |  
           ansible-playbook  playbook1-useradd.yaml -i /home/ronald/ansible/inventory.yaml --become
